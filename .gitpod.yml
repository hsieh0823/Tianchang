image: gitpod/workspace-full

tasks:
  - init: |
      sudo apt-get update && sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev unzip curl git
      flutter upgrade
  - command: |
      git config --global user.name "hsieh0823"
      git config --global user.email "you@example.com"
      flutter pub get

ports:
  - port: 3000
    onOpen: open-preview

vscode:
  extensions:
    - dart-code.flutter
